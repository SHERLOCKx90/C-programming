#include<stdio.h>
int main()
{
    char sherlock[20];

    printf("enter the address where u live:");
    scanf("%c",&sherlock);

    printf("the adress is :%c",sherlock);

    return 0;

}