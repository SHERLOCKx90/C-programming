#include<stdio.h>                                                               
                                                                                
void main()
{
    int a, b, *x, *y, temp;
    printf("\nEnter integer a:");
    scanf("%d", &a);
    x = &a;
    if (*x== 5 || *x==6)
    {
        
    }
    
    //prod = *x * *y;
    printf("\nMultiplication of the numbers: %d", prod);
}